{
      "compilerOptions": {
        "baseUrl": "./src", // Or "src" if your code is in a src folder
        "paths": {
          "@/*": ["./src/*"] // Example mapping @ to src folder
        }
      }
    }